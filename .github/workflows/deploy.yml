name: Deploy PBI-1 AND PBI-2 Sprint 2 test

on:
  push:
    branches:
      - pre-dev

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: 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

      - name: Auto Deploy container application itb-mshop 
        run: |
          set -x  
          cd /home/sysadmin/2-2567_INT221_KK-1 
          git pull origin pre-dev
          cd client
          npm i
          npm run build 
          docker compose down -v
          docker compose up -d --build
