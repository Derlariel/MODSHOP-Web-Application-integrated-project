
name: Deploy kk1 sprint 2



on:
  push:
    branches:
      - pre-dev

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Auto Deploy container application itb-mshop 
        run: |
          set -x  
          cd /home/sysadmin/2-2567_INT221_KK-1 
          git pull https://napatzzz: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@github.com/Derlariel/2-2567_INT221_KK-1.git pre-dev
          
          mkdir -p secrets
          cat > secrets/db_password <<EOF
          root
          EOF
          chmod 600 secrets/db_password
          
          docker compose down -v
          docker compose up -d --build
          docker image prune -f
          # docker builder prune -f 