
name: Deploy 

on:
  push:
    branches:
      - pre-dev

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Auto Deploy container application itb-mshop 
        run: |
          set -x  
          cd /home/sysadmin/2-2567_INT221_KK-1 
          git pull https://napatzzz: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@github.com/Derlariel/2-2567_INT221_KK-1.git pre-dev
          
          mkdir -p /tmp/db_secrets
          echo "root" > /tmp/db_secrets/db_password
          
          mkdir -p secrets
          cp /tmp/db_secrets/db_password secrets/db_password || true

          docker compose down -v
          docker compose up -d --build
          docker image prune -f
          # docker builder prune -f 